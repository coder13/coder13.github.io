var Model = require('ampersand-model');
var Timer = require('./timer.js');

module.exports = Model.extend({
    props: {},

    initialize: function () {

    },

    fetch: function() {
        
    }
})